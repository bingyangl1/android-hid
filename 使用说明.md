# LUOKE HID 使用说明

通过 Root Android 手机的 USB HID Gadget 实现硬件级键鼠模拟，绕过游戏反作弊（ACE）的软件注入检测。

## 目录
1. [原理](#原理)
2. [环境要求](#环境要求)
3. [手机端部署](#手机端部署)
4. [PC 端使用](#pc-端使用)
5. [完整键盘鼠标映射](#完整键盘鼠标映射)
6. [三层通信架构](#三层通信架构)
7. [配置项](#配置项)
8. [适配其他手机](#适配其他手机)
9. [常见问题](#常见问题)

---

## 原理

### 为什么需要硬件模拟
- Windows API 注入（`SendInput` / `PostMessage` / `mouse_event`）→ ACE 拦截
- 虚拟驱动（`ViGEm` / `vJoy` / `ScpVBus`）→ ACE 标记
- **真实 USB HID 设备** → ACE 无法区分（和你的真鼠标一样）

### 数据传输路径
```
  PC (HIDInput) ──USB线──→ 手机 (hid_daemon.py) ──→ /dev/hidg0/1 ──USB线──→ PC (HID设备)
      ↑                          |                           |
  控制命令                     ACM串口/TCP               鼠标/键盘报告
```

**关键**：控制和 HID 数据走同一条 USB 线，但零干扰——你的真鼠标/键盘继续用，手机发出的 HID 报告是独立的物理设备。

---

## 环境要求

### 手机
- **Root 权限**（Magisk 推荐）
- Termux + Python 3
- 内核支持 USB Gadget / configfs（2019 年后主流机型均支持）

### PC
- Python 3.8+
- `pip install pyserial`（可选，仅 USB 串口模式需要）
- SSH 客户端（兜底用）

---

## 手机端部署

### 推送文件
```bash
# 从 PC 推送到手机
scp -P 8022 phone/hid_daemon.py root@192.168.5.170:/data/data/com.termux/files/home/phone/
scp -P 8022 phone/reset_usb.py root@192.168.5.170:/data/data/com.termux/files/home/phone/
scp -P 8022 phone/exec.py root@192.168.5.170:/data/data/com.termux/files/home/phone/
scp -P 8022 phone/repeat_throw.py root@192.168.5.170:/data/data/com.termux/files/home/phone/
scp -P 8022 phone_hid_dual.py root@192.168.5.170:/data/data/com.termux/files/home/
```

### 启动 HID 模式
```bash
# SSH 到手机上执行
ssh -p 8022 root@192.168.5.170 "su -c 'python3 /data/data/com.termux/files/home/phone_hid_dual.py'"
```

脚本自动完成：
1. 停 adbd，释放 USB
2. 创建 USB Gadget（键盘 + 鼠标 + ACM 串口）
3. 绑定 UDC
4. 启动 hid_daemon.py（后台常驻）
5. PC 端出现 HID Keyboard / HID-compliant mouse / COM 串口

### 恢复 USB 为正常模式
```bash
python3 phone_hid_dual.py restore
```
或紧急恢复：
```bash
echo quit > /data/local/tmp/hid_daemon.quit
python3 phone/reset_usb.py
```

**单次使用后必须 restore**，否则手机连接电脑不再识别为存储设备/ADB。

---

## PC 端使用

### 基本用法

```python
from hid_device import HIDInput

# 自动选最快通道（USB > TCP > SSH）
dev = HIDInput()

# 鼠标
dev.mouse.click("left", 175)         # 左键，按住 175ms
dev.mouse.click("right", 65)         # 右键
dev.mouse.click("middle", 40)        # 中键
dev.mouse.click("x1", 40)            # X1 键（后退）
dev.mouse.click("x2", 40)            # X2 键（前进）
dev.mouse.press("left")              # 按住左键
dev.mouse.release()                  # 释放所有键
dev.mouse.release("left")            # 释放指定按键（其他键保持）
dev.mouse.move(100, -50)             # 相对移动 X+100, Y-50
dev.mouse.move(0, 0, 3)             # 滚轮+3（向下3格）

# 键盘
dev.keyboard.tap("A", 40)            # 按 A 40ms
dev.keyboard.tap("LSHIFT", 35)       # 按左 Shift
dev.keyboard.tap("F5")               # 按 F5
dev.keyboard.tap("ENTER")            # 按回车
dev.keyboard.press("W")              # 按住 W（走路）
dev.keyboard.release()               # 释放所有键

# 快捷键组合
dev.keyboard.press("LSHIFT")         # 按住 Shift
dev.keyboard.tap("A", 40)            # 按 A（同时 Shift 未松）
dev.keyboard.release()               # 松 Shift

# 丢球专用（洛克王国）
dev.cmd("throw:175:20:35")          # 左键175ms + Shift 35ms
dev.cmd("bthrow:10:175:20:35:0")    # 10次连丢, 球间0延时
```

### 强制指定传输层

```python
HIDInput("usb")         # 仅 USB 串口（需 pyserial）
HIDInput("tcp")         # 仅 WiFi TCP（需手机 daemon）
HIDInput("tcp:10.0.0.2:8023")  # 指定 TCP 地址端口
HIDInput("ssh")         # 仅 SSH（最慢，兜底）
```

### 原始命令（不经过键鼠封装）

所有 daemon 支持的命令可以直接发送：

```python
r = dev.cmd("mclick:x1:175")   # X1 鼠标按键
r = dev.cmd("ktap:CTRL:40")    # Ctrl 键
r = dev.cmd("ping")            # 健康检查 → "pong"
```

---

## 完整键盘鼠标映射

### 鼠标按键

| 名称 | 值 | 说明 |
|------|-----|------|
| `left` | bit 0 | 左键 |
| `right` | bit 1 | 右键 |
| `middle` | bit 2 | 中键 |
| `x1` | bit 3 | X1 键（鼠标前进） |
| `x2` | bit 4 | X2 键（鼠标后退） |

鼠标报告格式：`[buttons][X][Y][wheel]`（4 字节）

### 键盘按键

| 类别 | 支持的键名 |
|------|-----------|
| 字母 | `A`-`Z` |
| 数字 | `0`-`9` |
| 功能键 | `F1`-`F12` |
| 修饰键 | `LCTRL` `RCTRL` `LSHIFT` `RSHIFT` `LALT` `RALT` `LGUI` `RGUI` |
| 修饰别名 | `CTRL`(=LCTRL) `SHIFT`(=LSHIFT) `ALT`(=LALT) `GUI`(=LGUI) |
| 方向 | `UP` `DOWN` `LEFT` `RIGHT` |
| 导航 | `HOME` `END` `PAGEUP`(`PGUP`) `PAGEDOWN`(`PGDN`) |
| 编辑 | `BACKSPACE`(`BKSP`) `DELETE`(`DEL`) `INSERT`(`INS`) |
| 锁定 | `CAPSLOCK` `NUMLOCK` `SCROLLLOCK` |
| 数字小键盘 | `NUM0`-`NUM9` `NUM_DOT` `NUM_ENTER` `NUM_PLUS` `NUM_MINUS` `NUM_SLASH` `NUM_ASTERISK` |
| 其他 | `ENTER` `TAB` `SPACE` `ESCAPE`(`ESC`) `PAUSE` `PRINTSCREEN` `MENU`(`APPS`) |

### 组合键

```python
# 按下 → 按住 → 松开 三步走
dev.keyboard.press("LCTRL")    # 按住 Ctrl
dev.keyboard.tap("C", 40)       # 按下/松开 C
dev.keyboard.release()          # 松开所有键（含 Ctrl）
```

---

## 三层通信架构

| 传输层 | 延迟 | 需数据线 | 依赖 | 可靠性 |
|--------|------|---------|------|--------|
| **USB ACM** | ~0.1ms | ✅ | pyserial + COM 端口 | ★★★★★ |
| **TCP (WiFi)** | ~20ms | ❌ | 手机 daemon 运行中 | ★★★★ |
| **SSH (兜底)** | ~500ms | ❌ | 手机 SSH 服务 | ★★★ |

自动选择逻辑：`USB → TCP → SSH`

### USB ACM 模式（默认最优）

- 手机 daemon 监听 `/dev/ttyGS0`
- PC 通过 `pyserial` 自动发现 COM 端口（匹配 VID_22D9）
- 所有命令走 USB 线 bulk 传输，延迟 ~0.1ms
- 命令和 HID 数据走同一条 USB 线，不需要额外连接

### TCP 模式（WiFi 备用）

- 手机 daemon 监听 `0.0.0.0:8023`
- PC 通过 TCP socket 连接
- 适合调试、或 USB 不可用时

### SSH 模式（兜底）

- 不需要手机 daemon，直接 SSH 执行单条 Python
- `exec.py` 执行一次性的 Python 代码
- `repeat_throw.py` 批量丢球（旧方式）
- 延迟 ~500ms/次，每个 SSH 调用都重新打开 hidg0/hidg1

---

## 配置项

### 手机端环境变量

```bash
# 在 Termux 中 export 后运行 phone_hid_dual.py
export HID_UDC=a600000.dwc3        # UDC 名称（不设则自动检测）
export HID_CONFIGFS_PATH=/config/usb_gadget  # configfs 路径
export HID_TCP_PORT=8023           # daemon TCP 端口
export HID_DAEMON_PATH=/path/to/hid_daemon.py
```

### PC 端环境变量

```python
# 设置环境变量后 HIDInput 自动读取
os.environ["LUOKE_HOST"] = "root@192.168.1.100"
os.environ["LUOKE_SSHPORT"] = "22"
os.environ["LUOKE_TCPHOST"] = "192.168.1.100"
os.environ["LUOKE_TCPPORT"] = "8023"
os.environ["LUOKE_VID"] = "VID_18D1"  # Google/Pixel 的 VID
```

---

## 适配其他手机

### 1. 找 UDC 名称

```bash
ls /sys/class/udc/
# 一般只有一个设备: a600000.dwc3 (高通) 或 musb-hdrc (MTK) 等
# 如有多个，拔掉 USB 线再插，新增的就是
```

常见 UDC：
| SoC 厂商 | 常见 UDC 名 |
|---------|------------|
| 骁龙 865/870/888/8Gen1/8Gen2 | `a600000.dwc3` |
| 骁龙 8Gen3 | `a600000.dwc3` |
| 天玑 Dimensity | `musb-hdrc` 或 `11200000.usb` |
| 麒麟 Kirin | `ff100000.dwc3` 或 `ff200000.dwc3` |
| Exynos | `dwc3` |
| Tensor | `usb` |

### 2. 确认 configfs 已挂载

```bash
mount | grep configfs
# 如果没输出: mount -t configfs none /config
# 然后确认 /config/usb_gadget 存在
```

### 3. 确认 HID gadget 支持

```bash
# 如果 /sys/class/udc/ 有设备但 /dev/hidg0 创建不出来
# 检查内核配置: zcat /proc/config.gz | grep CONFIG_USB_G_HID
# 应为 =y 或 =m
```

### 4. 可能需要改 VID/PID

```bash
# 某些手机会覆盖 VID/PID 为 OPPO/Samsung 等厂商 ID
# 如果 PC 不识别，改 env:
export HID_VID=0x18D1  # Google
export HID_PID=0xDE00
```

### 5. 验证 HID 设备

```bash
ls /dev/hidg*
# 应出现 /dev/hidg0 (键盘) 和 /dev/hidg1 (鼠标)
# 如果只出现一个，检查 phone_hid_dual.py 中是否两个 function 都 link 到了 b.1
```

---

## 常见问题

### Q: PC 没有出现 COM 端口
**A**: 确认 `phone_hid_dual.py` 成功绑定了 `g.serial`。重启脚本或换数据线。某些厂商线是充电-only。

### Q: HID 设备出来几秒后消失
**A**: 手机内核的 USB 框架可能和 configfs 冲突。确认已完全释放 USB（`setprop sys.usb.config none` + `stop adbd`）。

### Q: 滚轮不动
**A**: 每秒滚多少次？windows 接收 -127 到 +127 的单次滚动量。

### Q: 键盘某些键没反应
**A**: 检查键名是否正确（用 `dev.keyboard.tap("F13")` 会返回错误）。看 daemon 日志。

### Q: 点不了 ACE 游戏里的按钮
**A**: ACE 可能读 USB 的 VID/PID 黑名单。尝试换 VID: `export HID_VID=0x046D`（罗技）。

### Q: USB 恢复后手机连电脑无反应
**A**: 关机重启。或在 Termux 里执行 `setprop sys.usb.config mtp,adb && start adbd`。

### Q: daemon 日志在哪看
```bash
cat /data/local/tmp/hid_daemon.log
```

### Q: 如何物理杀死 daemon
```bash
# 方法 1: 创建退出标记文件
touch /data/local/tmp/hid_daemon.quit

# 方法 2: 直接杀
pkill -f hid_daemon.py

# 方法 3: 终极方案
python3 /data/data/com.termux/files/home/phone/reset_usb.py
```
